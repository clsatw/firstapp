//  TypeScript guidelines discourage the I prefix.
// A class can act as an interface (use implements instead of extends).
// export interface Hero {
var Hero = (function () {
    function Hero() {
    }
    return Hero;
}());
export { Hero };
//# sourceMappingURL=prod.model.js.map